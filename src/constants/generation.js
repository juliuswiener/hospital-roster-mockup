// Plan generation constants

// Timeouts and intervals (in milliseconds)
export const GENERATION_PROGRESS_INTERVAL_MS = 500;
export const POLLING_INTERVAL_MS = 1000;
export const AUTO_SAVE_DEBOUNCE_MS = 2000;

// Default time limits (in seconds)
export const DEFAULT_TIME_LIMIT_SECONDS = 30;
export const QUICK_MODE_TIME_LIMIT = 30;
export const STANDARD_MODE_TIME_LIMIT = 300; // 5 minutes
export const OPTIMAL_MODE_TIME_LIMIT = 1800; // 30 minutes

// Progress simulation
export const PROGRESS_INCREMENT_MIN = 5;
export const PROGRESS_INCREMENT_MAX = 20;

// Display limits
export const MAX_SHIFTS_DISPLAY = 15;
